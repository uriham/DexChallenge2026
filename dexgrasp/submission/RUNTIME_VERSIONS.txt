Validated runtime for Model_UVLL_HanDex.ckpt
=================================================
Host OS: Ubuntu 22.04.2 LTS
Kernel: 6.8.0-136-generic
GPU observed during setup: NVIDIA GeForce RTX 3090, 24 GB
NVIDIA driver: 580.173.02
Driver-reported CUDA compatibility: 13.0
Challenge Conda environment: DexGraspMotionChallenge2026
Python: 3.8.19
PyTorch-bundled CUDA runtime: 11.3 (torch==1.12.1+cu113, per this repo's install.sh;
  no separate `cudatoolkit` conda package installed — the pip wheel bundles its own)
System CUDA toolkit (left unchanged, only used to compile native extensions): 12.6
GCC: 11.4.0 (/usr/bin/gcc) — note: this repo's README documents/targets gcc 8.4.0,
  but gcc-8 is not installed on this host and was not needed; pytorch3d,
  TorchSDF, pytorch_kinematics, and IsaacGym's gymtorch extension all built
  and ran correctly against system gcc 11.4.0.
G++: 11.4.0 (/usr/bin/g++)
PyTorch: 1.12.1+cu113
PyTorch Lightning: 1.8.1
NumPy: 1.21.0
OmegaConf: 2.1.1
Isaac Gym: Preview 4 (1.0.preview4)
pytorch3d: 0.7.9
TorchSDF: 0.1.0
pytorch_kinematics: 0.3.0
PyYAML: 6.0.3
SciPy: 1.10.1

Required runtime environment (verified — without this, IsaacGym fails to import):
  export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"

If Isaac Gym reports a missing libpython3.8.so.1.0:
  sudo apt update
  sudo apt install libpython3.8-dev
  (and/or apply the LD_LIBRARY_PATH export above — this is what fixed it here)

Other notes specific to this environment:
- Import order matters: isaacgym-touching modules (utils.config, utils.parse_task,
  etc.) must be imported before `import torch`, or IsaacGym raises "PyTorch was
  imported before isaacgym modules".
- `cfg/o6_hand_grasp_dexrep_ijrr.yaml` contains a non-ASCII (UTF-8) comment; if
  the shell's locale is not UTF-8, `yaml.load()` raises
  `UnicodeDecodeError: 'ascii' codec can't decode byte ...`. Fix by running with
  `PYTHONUTF8=1` (forces UTF-8 regardless of system locale).

Reproduce the full package list with:
  conda list -n DexGraspMotionChallenge2026 > RUNTIME_VERSIONS_full.txt
(this file lists only the packages relevant to running inference)
